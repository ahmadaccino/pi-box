---
name: hello-workspace
description: Inspect the /workspace directory and report what is there. Use when the user asks what files exist, what this computer can see, or to take a first look around.
license: MIT
---

# Hello workspace

You are running inside a container. The project files live under `/workspace`.

1. List `/workspace` with the `ls` or `bash` tool.
2. If it is empty, say so plainly and offer to create a file.
3. Keep the reply short.
