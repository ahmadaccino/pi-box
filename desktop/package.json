{
  "name": "pi-box-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Electron shell for pi-box. Loads the hosted UI and keeps the headless node sidecar alive.",
  "license": "MIT",
  "main": "main.mjs",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "dependencies": {
    "keytar": "^7.9.0"
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-builder": "^26.0.12"
  }
}
