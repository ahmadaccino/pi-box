{
  "name": "pi-box",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Open-source Grok Bot-shaped personal agent. Pi harness, any container, Cloudflare deploy.",
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:agent": "node container/server.mjs",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "node scripts/test-allowlist.mjs && node scripts/test-authenticate-url.mjs && node scripts/test-snapshot.mjs && node scripts/test-cdp-fail-closed.mjs && npm run test:mesh-place",
    "test:mesh-place": "node --experimental-strip-types --no-warnings scripts/mesh-place-check.mjs"
  },
  "dependencies": {
    "@clerk/backend": "^2.14.0",
    "@cloudflare/containers": "^0.0.30"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
